// gridflow starter library: cloud/AWS-flavored blocks
// Import with:  use aws

awsorange = #ff9900
awsdark   = #232f3e

class Service(name, color=#eef2f7) {
  shape = [rounded]
  label = $name
  fill  = $color
  icon  = "gear"
  port in  left
  port out right
}

class Lambda(name) {
  shape = [rounded, fill=#fff3e0, stroke=$awsorange]
  label = "λ $name"
  port trigger left
  port out     right
}

class Queue(name) {
  shape = [stadium, fill=#fce4ec]
  label = $name
  icon  = "queue"
  port in  left
  port out right
}

class Db(name) {
  shape = [cylinder, fill=#e8eaf6]
  label = $name
  icon  = "db"
  port write top
  port read  bottom
}

class Bucket(name) {
  shape = [card, fill=#e8f5e9]
  label = $name
  icon  = "box"
}

class Gateway(name) {
  shape = [hexagon, fill=#e0f2f1]
  label = $name
  icon  = "globe"
  port pub  left
  port priv right
}

class Cache(name) {
  shape = [stadium, fill=#e0f7fa]
  label = $name
  icon  = "bolt"
  port in  left
  port out right
}

class Cron(name) {
  shape = [rounded, fill=#f3e5f5]
  label = $name
  icon  = "clock"
  port fire right
}
